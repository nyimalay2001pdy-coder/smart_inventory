# Remove Advance Payment Module — Plan

## Overview
Completely remove the advance payment feature: delete new files, revert modified files, drop the DB table.

---

## 1. Files to DELETE

| File | Reason |
|------|--------|
| `purchase/advance_payment.php` | Main advance payment page |
| `ajax/get_supplier_advance.php` | AJAX endpoint for fetching advance credit |
| `ajax/link_advance_payment.php` | AJAX endpoint for linking advance payments |
| `migrate_advance_payments.php` | Migration script |

---

## 2. Files to REVERT

### A. `includes/sidebar.php` (line 114)
- Remove the advance payment menu item line

### B. `purchase/add.php` — revert all advance credit additions:

1. **PHP backend:** Remove `$advance_credit` variable and advance credit from payment status logic (lines ~73, ~90-98)
2. **PHP backend:** Remove the entire "Link advance payments" block (lines ~148-156)
3. **HTML:** Remove the "Advance Payment Credit" card (lines ~394-428)
4. **HTML confirmation modal:** Remove advance credit row (line ~547)
5. **JS variable:** Remove `advanceCreditData` (line ~708)
6. **JS functions:** Remove `loadAdvanceCredit()`, `toggleAdvanceCredit()`, supplier listener (lines ~933-987)
7. **JS `recalcTotals()`:** Revert to original without advance credit logic (lines ~992-1010)
8. **JS `showConfirmModal()`:** Revert to original without advance credit (lines ~1102-1123)

---

## 3. Database

Drop the table:
```sql
DROP TABLE IF EXISTS purchase_advance_payments;
```

---

## 4. Verification
- `purchase/add.php` works normally (no advance credit card, no JS errors)
- Sidebar Purchase menu shows only: New Purchase, History, Reports
- No 404 errors for removed AJAX endpoints
