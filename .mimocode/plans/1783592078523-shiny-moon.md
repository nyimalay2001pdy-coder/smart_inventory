# Fix Sun/Moon Theme Icons

## Problem
Sun icon should display in light mode, Moon icon in dark mode. The logic in `app.js` and the inline script in `header.php` is already correct, but the SVG icons may not be rendering.

## Files to Modify

### 1. `includes/header.php` (lines 58-86)
Replace the current filled SVG icons with clean, well-tested Heroicons stroke-based SVGs that are guaranteed to render. Remove the inline `<script>` that tries to set initial icon state — move that logic into `app.js` instead to avoid duplication.

### 2. `assets/js/app.js` (lines 1-40)
- Keep the `setIcon()` and `toggleTheme()` functions as-is (logic is correct)
- Add a `DOMContentLoaded` listener to call `setIcon(currentTheme)` on page load, ensuring the correct icon displays immediately

## Verification
1. Open any page in browser
2. Dark mode (default) → Moon icon should be visible
3. Click toggle → Light mode → Sun icon should appear with smooth animation
4. Refresh page → Correct icon persists from localStorage
5. Navigate to different page → Icon stays correct
