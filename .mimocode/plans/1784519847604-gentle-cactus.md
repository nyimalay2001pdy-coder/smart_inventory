# Plan: Improve Profit Calculation and Storage Logic

## Current State Analysis

After thorough code review, the system **already implements most of the requirements correctly**:

### What's Already Working (No Changes Needed)

| Requirement | Status | Location |
|---|---|---|
| 1. `profit` column in `sale_details` | **Already exists** | `config/migration.sql:161` |
| 2. Profit calculated at sale time: `(selling_price - purchase_price) * quantity` | **Already implemented** | `sale/pos.php:168-177` |
| 3. `purchase_price`, `selling_price`, `quantity`, `subtotal`, `profit` saved to `sale_details` | **Already saved** | `sale/pos.php:174-177` |
| 4. Stored values never change after save | **Already the case** (snapshot in `sale_details`) | `sale_details` table |
| 5. Reports use stored `profit` from `sale_details`, not recalculated | **Already using stored values** | `sale/reports.php:40`, `reports/index.php:141`, `dashboard/index.php:20-22` |
| 7. Profit displayed in Dashboard, Sales Report, Profit Report, Admin Analytics | **Already displayed** | `dashboard/index.php:20-22`, `sale/reports.php:37-44`, `reports/index.php:136-156` |
| 8. Existing sale/stock/payment logic unchanged | **No changes needed** | `sale/pos.php` sale flow is untouched |

### What Needs Fixing

| Requirement | Status | Location |
|---|---|---|
| 6. Profit must NOT display on Customer Invoice / Printable Receipt | **Currently showing profit** | `sale/invoice.php:144, 157-160, 187-189` |

## Changes Required

### File: `sale/invoice.php`

This is the **customer-facing invoice and printable receipt** (has a Print button at line 211). Profit should be removed from here per requirement 6.

**Change 1**: Remove the `Profit` column header from the items table (line 144)
- Delete: `<th class="num">Profit</th>`

**Change 2**: Remove the profit cell from each item row (lines 157-160)
- Delete the `<td>` block that displays per-item profit

**Change 3**: Remove the profit summary line from the payment summary section (lines 187-189)
- Delete the `<div>` that shows total profit/loss

**Change 4**: Remove unused `$total_profit` variable (lines 33, 37)
- Remove `$total_profit = 0;` and `$total_profit += $row['profit'] ?? 0;`

### No Other Files Need Changes

- `sale/pos.php` - Sale creation logic is already correct and must not be changed per requirement 8
- `dashboard/index.php` - Already uses stored `sd.profit` correctly
- `sale/reports.php` - Already uses stored `sd.profit` correctly
- `reports/index.php` - Already uses stored `sd.profit` correctly
- `sale/history.php` - Admin modal view (not customer-facing), profit display is acceptable here

## Summary

The system's profit calculation and storage logic is already sound. The only change is hiding profit from the customer-facing invoice (`sale/invoice.php`) to comply with requirement 6. This is a minimal, targeted change that does not affect any sale, stock, or payment logic.
